# exercises/

Written work, before it is cleaned up into a note. Markdown, or photos of
paper — either is fine.

Naming: `phaseN-chapterNN-exNN.md`, or whatever is convenient. This directory
is a workspace, not an artifact; `notes/` is the artifact.

Keep wrong attempts. They're the input to section 3 of the chapter note.
